# Указатель журналов и сертификатов

**Правило (принято обоими солверами):** любое число в тексте несёт ссылку на файл и строку журнала,
которые его дали. Число без ссылки считается НЕПРОВЕРЕННЫМ независимо от того, кто его написал.

Синхронизация с вычислительных машин: `slack/targets/sync_journals.sh`. Запускать перед каждым
коммитом, где появляются новые числа. Существует потому, что незалитый журнал мы уже теряли.

## Что где лежит

| каталог | содержимое |
|---|---|
| `logs/sweeps/` | прогоны по гиперболам (k = ±1, пары, четвёрки) — закрытое направление |
| `logs/no3_3d/` | no-three-in-line в кубе: доказательство n = 5, нижние границы n = 6…10 |
| `logs/a280537/` | без четырёх компланарных: калибровка n = 2…5, оценки стоимости n = 5…7 |
| `certs/a280537/` | свидетели-конфигурации и **независимый** проверяльщик `verify_witness.py` |
| `certs/no3_3d/` | свидетели для трёхмерного no-three-in-line |

## Отрицательные результаты — хранятся наравне с положительными

- `logs/no3_3d/vm2_n5_results_VOID_killed.txt` — 150 строк, ПРИЗНАНЫ НЕДЕЙСТВИТЕЛЬНЫМИ: процессы были
  убиты `pkill`, обёртка засчитала пустой вывод как успех. Ловушка «killed ≠ finished», из-за неё
  переписана обёртка (`run_prefix.sh` печатает OK/FAIL) и проверялка покрытия.
- Направление «константа для пары гипербол» закрыто как исчерпанное на 3.44817(p−1); все локальные
  маршруты сертификата измерены и упёрлись в потолок. См. `docs/ROADMAP_hyperbola_archive.md`.
- Полная перепись rot2 при n = 32 признана невозможной: ≈5.7·10¹⁸ узлов, разрыв ~2·10⁵ раз не
  закрывается ни числом машин, ни оптимизацией. См. дополнение к брифу 9.
- Первая версия `plane4.c` (перечисление плоскостей по коэффициентам) давала n = 4 → 11 вместо 10;
  ошибка и её причина описаны в `docs/research/targets/audit_A280537.md`.
- Ресёрчи 10 и 11: все 25 рассмотренных кандидатов отвергнуты. Это результат, а не провал —
  см. `docs/research/deep_research_11_answer.md`.

## Смена 22 августа 2026 — гипотеза Гая–Келли и процедурные уроки

`logs/kelly/` — новый каталог. Здесь лежат числа, на которых стоит `paper/guy_kelly_error.tex` v1.6.

| файл | что в нём, и какое число он держит |
|---|---|
| `kelly/константа_подтверждена_и_проверена_счётом.txt` | замкнутый вывод **α² = π²/3**; число Гая — корень α³ = 2π²/3, куб на месте квадрата. Асимптотика **T(n)/n⁴ = (3/π²)·ln n − 0.2545**, коэффициент до шести знаков (прирост на удвоение 0.210690 против (3/π²)ln2 = 0.210691). Численный предел порога **1.8138 ± 0.002** на двух независимых реализациях |
| `kelly/промах_зависит_от_формы.txt` | промах эвристики при **одном и том же n=20**: +15.88 на потолке, +3.30 у порога, −0.25 при r=0.8. Катастрофа принадлежит потолку, а не эвристике |
| `kelly/раздел6_отзывается_вопрос_сузился.txt` | тринадцать сигм раздела 6 стояли на **пяти попаданиях** при n=20, r=0.9. Значения не опровергнуты — необеспечены |
| `kelly/различение_недопустимо.txt` | Θ(n) против Θ(n·ln n) не различается: коллинеарность 0.9983 и **не улучшается** с ростом предела (0.9993 до n=10⁴). Четыре формы проходят на одном хвосте и отвечают противоположно |
| `kelly/погрешность_межреализационная.txt` | **разброс между семенами не есть погрешность**: две реализации расходятся до 5.5 раз сильнее заявленного, и расхождение растёт с n (1.9, 2.5, 1.6, 0.2, 3.2, 3.2, 5.5) |
| `kelly/сырые/` | все прогоны оценщика, на которых стоят таблицы выше |

Трёхмерная таблица и проверки:

| файл | что в нём |
|---|---|
| `a280537/независимая_проверка_n10_завершена.txt` | n=10 исчерпан независимой реализацией: **1 522 445 871 узел, 8.93 ядро-часа**, максимум 26 в каждой доле. Плюс калибровка планкой 25: семь долей из восьми достали до 26 |
| `a280537/покрытие_n10.txt` | публикуемое состояние покрытия — пишет тот, кто ЗАКРЫЛ долю. Заведено потому, что вопрос о состоянии стареет за минуты |
| `a280537/пятнадцатая_вторая_точка_слабая.txt` | вторая точка по n=15 есть, но **одна доля из шести** достаёт до известного — свидетельство близко к нулю по силе, и слабость названа числом |

Процедурные уроки этой смены:

| файл | правило |
|---|---|
| `невопрос_хуже_проваленной_проверки.txt` | **планка ставится на единицу НИЖЕ известного.** Планка, равная известному, превращает отчёт в эхо. Инструмент, не спрошенный о калибровке, хуже проваленной калибровки: провал виден, невопрос нет |
| `проверка_процессов_врала_нулём.txt` | `ps -eo comm=` печатает полный путь; все проверки живости молча возвращали ноль при шести живых процессах. И первая редакция этого урока потерялась молчаливым провалом записи |

Ещё два правила выведены и записаны в переписке, здесь для полноты:
* **планка безвредна при исчерпании и опасна при обрыве** — у исчерпанного дерева она сужает вопрос, у оборванного подменяет ответ («не нашёл» и «не успел» изнутри неразличимы);
* **значимость коэффициента измеряется внутри модели и не переносится на выбор между моделями** — проверять надо не «отличим ли коэффициент от нуля», а «есть ли вторая непротиворечивая модель, где его нет».
