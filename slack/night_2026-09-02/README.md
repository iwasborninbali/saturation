# 2 сентября 2026 — что измерено за день и что запущено на ночь

Все скрипты читают `web/all_known_solutions` (база Фламменкампа, состояние 31.08.2026; формат — `web/decode.py`).
Числа ниже воспроизводятся запуском соответствующего скрипта; журнал ставок — `~/projects/moves/ledger.jsonl`
(строки `by: opus (сессия saturation, 02.09)`).

| скрипт | что измеряет | результат |
|---|---|---|
| `proj_pilot.py` | проекция решений на диагональ: доля дважды занятых диагоналей κ(n) | κ = 0.731 ± 0.002, ровно от n=12 до 57 |
| `proj_null.py`, `kappa_null.py` | нуль-модели для κ (случайные 2/строку+2/столбец; max-entropy «≤2 на диагональ») | нуль 0.639 при n=20, предел 0.6486; решения на 13 % выше |
| `spectrum.py`, `dirratio.py` | спектр направлений: пары точек по примитивным направлениям против ожидания для случайного множества | (1,0) 0.54, (1,±1) 0.61, (1,±2) 0.78, (1,±3) 0.94, m=4 1.08, m=5–7 1.16–1.18 — короткие обеднены, длинные обогащены |
| `trend.py` | κ(n) по всей базе с подгонкой K + c/n | K = 0.732 (кандидаты √3−1, 2−4/π не различимы) |
| `proj3d.py` | куб: «нет 4 компланарных» ⟺ «нет 4 коллинеарных в каждой проекции», проверка на 19 свидетелях A280537 | подтверждено |
| `prune_power.py` | сила отсечения по κ и по «коротким парам» на префиксах решений против случайных законных (n=12) | AUC 0.40–0.47 / 0.27–0.30; отсечение ≤ 7 % / ≤ 19 % при потере 5 % решений |
| `certificates.py`, `certificates2.py` (+ `certificates2_n12.log`) | чем удостоверяется смерть ветки: счёт по строкам, Холл, просмотр на шаг, истина | наверху дерева (глубина 4 из 12) смерть невидима никому, цена доказательства 39–1354 узла; внизу видна на 97 % |
| `layers.py` (+ `layers_n10.log`) | полное дерево обхода n=10 по слоям: узлов / живых | пик 2.7 млн узлов посередине при 1 089 живых; хвост вынужден |
| `rct4_census.py` | перепись 2 685 rct4-решений: пара на главной диагонали, смещение d, карта плотности | ровно одна пара на главной диагонали всегда; d/n широкое (пик 0.35–0.45); середины сторон ×1.6, углы ×0.1–0.3 |
| `extend2.py`, `extend2_verify.py` | достройка n → n+2 без сдвига старых точек (9 размещений) | 76 из 431 008 достраиваются, все — четыре угла, класс rot4, все уже в базе (+1 невозможна: доказательство в чате/плане) |
| `algcores.py` (+ `algcores_results.json`) | алгебраические ядра: максимум |S ∩ C| по модулярным гиперболам и коникам против нуль-модели, n=8…40 (ВМ, 817 с) | решения НИЖЕ нуля при всех n; ядер нет — затравки из коник закрыты |
| `cycles.py` | циклы 2-фактора решений по классам (покрытие дефектных семейств) | 2-цикл у 29 % iden / 37 % rot2 / 14–18 % dia1. **Поправка вторы (23:55):** нуль в этом скрипте (пара случайных перестановок) весит конфигурации как 2^(число циклов) и потому неверен; точный равномерный нуль (`vtora/cycle_null.py`, сверен с A001499) даёт при n=20 P(гамильтонов)=0.333, P(2-цикл)=0.231 — у решений 0.307 и 0.287: длинные циклы НЕ предпочтительны, а прямоугольник обогащён ×1.2; циклы длины 3…10 — как в нуле. Тот же смещённый сэмплер стоит в `proj_null.py` (нуль-A) и `prune_power.py`; закрытая формула `kappa_null.py` от него не зависит |
| `cube_strata.py` | **ночной счёт**: A399138, точный максимум внутри каждой страты симметрии куба (CP-SAT), n=7, 8 | идёт на ВМ; дымовой тест n=5 → 40 = a(5) |

| `spectrum_model.py` | ночь: спектр как цена затенения, pairs ∝ E·exp(−λ·kill), λ по диагоналям | RMS 0.05 по 12 уровням, но **противник (Opus): ранен тяжело** — при m ≥ 10 kill ≡ 0 (константа по построению), λ + константа на m ≥ 4 даёт RMS 0.029 (kill вредит), линейная цена не отличима от экспоненты, якорь на диагоналях испорчен потолком pairs ≤ 20 при E' = 25.3; выжило: обеднение m=2,3 из m=1 одной ценой; Гай–Келли (0 параметров) даёт форму и пересуживает ×1.7. Ставка перескорена «нарушен» |

Ночная смена: `docs/ПЛАН_12ч_2026-09-02.md`. Прайор-арт по спектру направлений — `docs/research/deep_research_brief_12_direction_spectrum.md`.
