# Отчёт: насыщение (no-three-in-line) — с нашего конца

*Живой документ; обновляется по мере результатов. Все числа — из журналов в `bench/`, все книги — через `saturation.certify`.*

## 1. Задача
`saturation.py` (v1) — слепая формулировка: токены n×n, закон «никакая тройка не вырождена», потолок 2n, насыщение = 2n,
«какие n насыщаются — не записано». Узнана: no-three-in-line (Дьюдени, 1917); 2n-книга = 2n маркеров без трёх на прямой.
Партнёру дали v2 с TARGET=71 (плацдармы); киты объединены в этом репозитории.

## 2. Инструменты (все — здесь, `make && make check`)
- `there.c` — точный поиск: битово-параллельный DFS с рестартами, MRV по строкам и столбцам, затенение всех пар,
  орбиты симметрии (iden/rot2/rot4/dia1/ort1/ort2/dia2/full/rct4), маски попарно несовместимых орбит, полное перечисление,
  достройка при фиксированных токенах (`FIX=`), rot2-пары внутри класса четверти оборота (`PAIRS=`), смешанный класс (`sym 11`).
  `there128` — до n=128. Валидация: A000769 (n=2…10: 1,1,4,5,11,22,57,51,156 ✓), таблицы Фламменкампа (dia1 n=11/13/15: 12/20/28;
  dia2 n=11…25: 0,2,2,0,0,0,2,4; rct4 n=9…21 с точностью до множителя 2 — зеркало диагоналей ✓), трассы `bt.c` партнёра ✓.
- `there.py` — драйвер (параллельные сиды, классы, `books.json`), `there_sat.py` (kissat), `lns.py`, `web/decode.py`,
  `web/stab.py` (точный класс + кодировка базы; round-trip на n=76 ✓), `bench/*` — калибровки.

## 3. Карта сертифицированных книг (`books.json`)
n = 2…30, 32, 34, 36, 38, 40 (rot4/dia2/rot2/rct4/full — по классам), 41 (rct4); очередь: 31, 33, 35, 37, 39 (rct4), 42, 44 (rot4).
Каждая запись: токены, класс, сид, узлы, секунды, сертификат.

## 4. Измерения и законы роста
- Полные деревья: rot2 ×10 на +2 (n=18: 4.9·10⁷ узлов, 1330 книг), dia1 ×9 на +2 (n=19: 7.7·10⁷, 32 книги), rot4 ×20 на +4
  (n=28: 1.2·10⁷, 116 книг), dia2 ×4 на +2 (n=29: 3.2·10⁶, 2 книги).
- Стоимость одной книги: rot4 ×8–10 на +4 (n=36: ~10–60 с на 8 сидах; 40: 70 с), rct4 ×~10 на +4 (41: 806 с; 43: 6.7 ядро-ч
  у партнёра; 45: >3.3 ядро-ч без книги), rot2 ×6 на +2 (24: 10 с; 27: 137 с на 8 сидах).
- Сравнение: CP-SAT Преллберга (arXiv 2602.07751) — то же ×10 на +4, константа хуже нашего DFS в разы; n=60 у него ≈ 60 ядро-лет.

## 5. Состояние в мире (11.08.2026) и вердикт
Известны все n ≤ 70 и 72, 74, 76 (Хойле — SAT; Преллберг — CP-SAT/GPU-код Райли); открыты 71, 73, 75, ≥77.
Инструменты рекордсменов непубличны. Экстраполяция нашей кривой к 71: 10⁵–10⁷ ядро-часов ⇒ облако не арендуем, 71/78 — не наша гонка.

## 6. Отрицательные результаты (все воспроизводимы)
- Локальный поиск на ровно 2n (min-conflicts, SA, ILS): умирает при n≈12–28; плацдармы 128–134 при 71 не достраиваются
  (`FIX=` → «none» мгновенно).
- kissat (наша CNF): проигрывает DFS с n≈27.
- Тёплый старт/LNS от книги n к n+2: 44→46 не достраивается даже при 7 сохранённых орбитах из 22 — соседние размеры не коррелированы.
- «rot4 + одна rot2-пара вне диагонали» — пусто (лемма о балансе, доказана независимо обоими); «C4-орбиты + ≥1 V2-четвёрка»
  — пусто при n≤33; BP-децимация без бэктрекинга — умирает при n≥8.

## 7. Положительные находки
- Лемма о балансе (общая формулировка партнёра): дефект относительно H с движением столбец→строка — эйлеров орграф на классах строк.
- Новая книга с точной симметрией rot2 при n=33 из семейства «C4-орбиты + 3-цикл rot2-пар» (партнёр; `docs/SUBMISSION.md`);
  клетка «:» при 33 в таблице не пуста (`..` + `n33.gif`), найденная книга не эквивалентна изображённой.
- Чётное семейство «C4-орбиты + петля на главной диагонали + петля на побочной» непусто: n=24 — 8 книг, 26 — 4, 28 — 4;
  **n=36 — первая известная книга с точной rot2** (подкласс x=6, y=5; 103 с).
- Семейство «C4-орбиты + 3-цикл rot2-пар»: **n=37 — первая точная rot2** (партнёр; затем вторая), **n=39 — первая точная rot2**
  (моя половина развёртки, подкласс 4,8;8,20;20,34). V2-база пуста при 37/39/41 (полностью). Пустые «:» закрыты: 36, 37, 39; идёт 41.

## 8. База Фламменкампа
Кандидаты — `docs/SUBMISSION.md`; отправка — решение хозяина.

## 9. Совместная работа
`docs/HELLO.md` → `docs/REPLY.md` → `docs/THREAD.md` (по порядку); задачи разделены (партнёр — 37/39 и семейства циклов, я — 36/41 и карта).

## 10. Состояние на утро 19.08.2026 (второй солвер; ночь автономной работы)
**Статьи.** Обе прошли внешние аудиты (ChatGPT deep research; тексты в `docs/reviews/`). Заметка HJSW — v0.6: закрыт единственный пробел
(подсчёт 144 в лемме о гаджетах — доказан явно, независимая проверка), приоритет/библиография/теги; добавлено следствие «среди коник
только сдвинутые гиперболы могут достичь 3(p−1) в боксе 2p×2p». Основная статья — v0.7: исправлены две реальные ошибки (rct4 уже имеет
точную rot2 ⇒ «первые» только при 36, при 37/39 — «первые не rct4»; генератор 3‑циклов пропускал центральный класс — списки и все
развёртки пересчитаны с полными журналами в `logs/`), лемма уточнена (каноническая база, полная классификация дефектов ≤ 3 пар),
формула валидации исправлена. Оба PDF на столе владельца; теги `defects-v0.7`, `hjsw-note-v0.6`; `docs/RELEASES.md` с SHA‑256.
**Новые книги.** n=36: три (двухпетлевое семейство исчерпано: ровно 3); n=39: три (две — из центральных 3‑циклов, найдены только после
исправления генератора); n=37: ровно две (семейство закрыто); n=33: одна. Письмо №2 Ахиму отправлено (в его треде).
**Второй вектор — теорема о паре гипербол (цель хозяина «3 балла»).** Записано в `docs/research/pair_bound_notes.md` (14 разделов, три
агента): (а) структура точных максимумов: сбалансированы между гиперболами, 1–2 копии на класс, пары в одном столбце/строке, потеря от
тривиальной оценки 4(p−1) ровно p−6 при 11/13/19; (б) переформулировка как задача ориентаций (3‑CNF на p−1 битах); (в) LP‑сертификаты
на всех богатых прямых бессильны (x≡½), нужны прямые с ≥5 точками; (г) **открытие третьего:** при k=−1 LP только по строкам/столбцам/±1
даёт ≈3.45(p−1) при всех p ≤ 199, при p=29, 37 двойственное — целочисленное РАЗБИЕНИЕ (блоки = группы вычетов с 8‑точечной прямой:
8+4+4); (д) арифметика 8‑точечных прямых: кубика a b²+(a²−1)b+a≡0, условие на наименьшие вычеты, главный член m₈ ~ p/12 (данные до
p=1500: 0.083; экономия → (p−1)/2 на наклон); (е) схема теоремы T3′: α(P₋₁) ≤ 4(p−1) − 4m₈ + O(1) ≈ 3.67(p−1) (или 3.5 с 6/7‑точечными) —
арифметика стандартна (Вейль/Бомбьери), комбинаторика покрытия — конечный разбор случаев (главная работа); (ж) для общих k LP(1) тоже
< 4(p−1) при больших p (3.6–3.9), но слабее. Внешние агенты хозяина (ChatGPT, лимит до 25.08) дали два верных замечания (коники; линейные
компоненты O(√p)) и аудит «Gate B» — учтено; T1–T4 никем не доказаны.
**Идёт.** ВМ‑2: центральные подклассы 41 (C4, медленно) → 45 → полные пересчёты 39, 41; CP‑SAT p=23 (12 ч); модель R p=17…37. Мак:
CP‑SAT p=29 (8 ч). Мониторы: коммиты, ВМ‑2, почта.
**Решения владельца.** (1) T3′ — делать (несколько дней третьего + проверка) или нет; моя рекомендация — делать. (2) `docs/HUMAN_AUDIT.md`
— личная проверка перед подачей. (3) Квота GCP (32 vCPU в проекте исчерпана ВМ‑2; ВМ‑3 не поднялась) — при желании запросить увеличение
CPUS_ALL_REGIONS в консоли.
