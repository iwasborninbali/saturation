# Передача работы соавтору — что доказано, что вычислено, чему верить нельзя

Состояние на 2026-08-20. Два законченных текста, оба собираются из репозитория без ошибок.
Этот файл — единственное, что нужно прочесть перед тем, как ставить своё имя под текстом.

## 1. Две работы

**A. `paper/hjsw_window.tex`** (тег `hjsw-note-v1.14`, 45 стр.) — заметка о модулярной гиперболе в окне HJSW.
**B. `paper/no3inline_defects.tex`** (тег `defects-v1.1`, 11 стр.) — конфигурации без трёх на прямой с точной полуоборотной симметрией.

## 2. Что ДОКАЗАНО (обычные математические доказательства, проверяются чтением)

- Теорема окна: в коробке 2p×2p допустимое подмножество одной гиперболы xy ≡ c имеет не более 3(p−1) точек; классификация всех богатых
  прямых; число максимумов ровно 9^s; точная формула для ЛЮБОЙ коробки; следствие для no-four-in-line (7/2(p−1)+s).
- Устойчивость: множество размера 3(p−1) − t отличается от некоторого максимума не более чем в t точках (резко).
- Пара гипербол: α ≤ 4(p−1) − 4m₈ (Theorem two); семь-точечные блоки; блочное разложение по сериям квадратичных вычетов;
  закон спусков для типов серий; итоговая константа α ≤ 427101431/123863040 (p−1) = 3.44817(p−1).
- Кубические графики: не достигают значения гиперболы; сильная форма ≤ (4/3+o(1))·2p.
- Лемма баланса для симметричных дефектов (работа B).

## 3. Что ВЫЧИСЛЕНО доверенной программой (не доказательство; принимается на веру к коду)

- n = 41: все 4750 подклассов трёхциклового семейства исчерпаны в каждой из двух баз, конфигураций нет. Журналы `logs/sweeps/f41A_sym{9,10}_n41.txt`.
  ВАЖНО: это результат ПО СЕМЕЙСТВУ, а не перепись всех rot2-конфигураций при n = 41. Формулировка в тексте это отражает.
- n = 36 (двухпетлевое семейство): три конфигурации; подкласс {1,4} доисчерпан отдельным запуском без лимита (48.4 млн узлов, решений нет),
  запись добавлена в `logs/sweeps/twoloop_n36_exh.txt`.
- Четыре новые конфигурации (n = 36, 37, 39) опубликованы А. Фламменкампом в его базе.

## 4. Что ПРОВЕРЕНО НЕЗАВИСИМО (две-три реализации, написанные отдельно друг от друга)

- Семейные режимы решателя (sym = 9/10): независимый перечислитель, n = 13 — 20/20 и 13/13 подклассов, включая 25 нулевых.
- Режим sym = 2 с парами (чётные n): n = 14 — 21/21, n = 24 — 66/66, включая оба подкласса с решениями.
- Блочная константа: таблица оптимумов получена тремя независимыми способами, свёртка — двумя; точное совпадение дробей.
- Устойчивость: три независимые реализации.
- Числа для одной гиперболы: сверка с OEIS A000769 и со счётчиками базы Фламменкампа.

## 5. Чему верить НЕЛЬЗЯ (ограничения, которые обязаны попасть в текст)

- **Режим sym = 2 БЕЗ пар — не перечислитель.** При n = 8 он выдаёт 8 конфигураций из 36. Для утверждений статей не использовался,
  но если кто-то возьмёт код для новой задачи — это ловушка. В тексте оговорено.
- **Константы раздела 10 заметки** (γ₁, γ₂) — численная квадратура явных кусочно-полиномиальных интегралов, не интервальное вложение.
  Запас над порогом больше десятикратной оценки ошибки; сказано прямо.
- **Гипотеза α ≤ 3(p−1)+6** — это гипотеза, а не теорема. Данные исключают эффект нужной величины почти на порядок,
  но НЕ различают ограниченный выигрыш и линейный с наклоном ≤ 0.1. Все точные значения известны только для малых p.
- **Предасимптотика.** Внутри наших же данных есть величина (межблочный дефицит), которая при доступных p выглядит константой
  и доказуемо линейна. Это предупреждение относится ко всем выводам из малых случаев.
- **Приоритет.** Значение 3(p−1) заявлено Ковачем–Надем–Сабо без доказательства; им написано, ответа нет. Фраза «первое, насколько нам
  известно, полное доказательство» должна быть перепроверена перед подачей.

## 6. Внешний аудит

Работы проходили два внешних LLM-аудита и два дип-ресёрча. Последний (№9) нашёл в релизе v1.0 три дефекта: незакрытый подкласс при n = 36,
ссылку на устаревший тег, и неоднозначность формулировки про n = 41. Все три исправлены в v1.1. Отчёты: `docs/research/`.

## 7. Что делать соавтору

Тексты готовы по содержанию. От человека нужно: научная редактура и сокращение (заметка разрослась до 45 стр.), решение о разделении на
две-три статьи, выбор журналов, проверка приоритета относительно KNS, и — по желанию — усиление аналитической части (константы раздела 10
интервальной арифметикой). Вся вычислительная часть воспроизводима из репозитория; скрипты и журналы перечислены в разделах верификации обеих работ.
