# План на 12 часов — ночь 2→3 сентября 2026 (23:00 → 11:00 WITA, Бали)

Владелец: «запускаем куб считаться; когнитивная работа — открытия, изобретения, новая алгебра, новые подходы;
пусть ничего не получится — ничего страшного; фоном — новые решения для таблицы Ахима». Отчёт каждые 30 минут:
`ψ→`, время по Бали, ссылка на этот файл, состояние счёта, что сделано в когнитивном цикле.

## Машина

`saturation-alg-1` — c2d-highcpu-32, us-east4-b, проект loyobondar-prod, аккаунт `saturation-agent@…`.
Срок жизни продлён до 13 ч (`max-run-duration`, действие DELETE — снесёт себя сама). **Сносить руками — после
11:00 WITA, когда всё скачано.** Вторую 32-ядерную поднять нельзя: квота CPUS_ALL_REGIONS = 32 (moves/docs/agents.md).
Файлы на ВМ: `~/all_known_solutions` (база Ахима, 31.08), `~/there` (собран), `~/cube_strata.py`, `~/strata/`.

## Вычислительная дорожка (32 ядра)

1. **Куб, A399138, точный максимум по стратам симметрии** — `cube_strata.py` (CP-SAT, OR-Tools): для каждого
   класса сопряжённости подгрупп O_h порядка 2…48 — максимум инвариантных конфигураций без трёх коллинеарных в [n]³;
   свидетель перепроверяется полным перебором троек. n=7 (наш свидетель 73), затем n=8 (93). Логи `~/strata/n7.log`,
   `~/strata/n8.log`, результаты `~/strata/n{7,8}_results.json`, свидетели `~/strata/n7_c*_*.txt`.
   Ставка до счёта: a(7) ≥ 76 (60 %), a(8) ≥ 95 (50 %).
2. **Таблица Ахима, пустые клетки** — `there` с сидами и длинным лимитом: iden при 33/35/37 (`sym=0`), dia1 при 33
   (`sym=3`), rot2 при 41 (`sym=1`). Логи `~/table/*.txt`; каждая находка — строка `book …` → проверить `stab.py` и
   сертифицировать, потом в `docs/SUBMISSION.md`.
3. Ядра делятся: куб ~16, таблица ~16; когда куб при 7 закончится — всё на 8, потом на таблицу.

## Когнитивная дорожка (каждые 30 минут, крон)

Цикл: (1) состояние счёта с ВМ — одной строкой на дорожку; (2) одна попытка мысли — линза из `moves/lenses/INDEX.md`
(по одной за цикл, не больше трёх карточек), или своя; объект — один из вопросов ниже; (3) если родилась проверяемая
гипотеза — предсказание числом до счёта в `moves/ledger.jsonl`, проверка (на ВМ, не на Маке), исход; (4) отчёт.
Противник — подагент (Opus) до приёмки чего-либо как находки. Отрицательный результат — законный выход.

Вопросы (в порядке ставок):
- **Механизм спектра направлений.** Закон «короткие направления обеднены, длинные обогащены» — вывести число из модели
  «пара стоит затенённых клеток»: max-entropy с весом exp(−λ·затенение). Совпадёт ли кривая 0.54/0.61/0.78/0.94/0.99/1.08/1.17?
- **κ = 0.731 из модели.** Тот же max-entropy, но с ограничениями по всем направлениям (не только диагональ): даёт ли 0.73?
- **Длинные циклы.** Решения предпочитают один гамильтонов цикл (26–31 % против 9–13 % у случайных). Почему? Есть ли
  теорема: короткий цикл 2-фактора порождает много коллинеарных троек в среднем?
- **Жёсткость как алгебра.** «Удаление любой точки оживляет ровно одну клетку» — это свойство максимальности или
  насыщенности? Формулировка как лемма и контрпример при неполной насыщенности.
- **Куб: слоистая структура.** 2n²−2n+4 при малых n — вывести конструкцию, дающую эту величину при n=7 (88), или
  показать, что она ломается.
- **Проективный взгляд.** Точки (x, y, 1) ∈ Z³, «нет трёх зависимых»: есть ли перенос техник из no-3-collinear в кубе
  (A399138) обратно в плоскость, и наоборот?

## Правила

- На Маке ничего тяжёлого не запускать. Счёт — ВМ.
- Число без журнала — непроверенное. Ставки — до счёта, числом с допуском (`moves/ledger.jsonl`).
- Письма никому не отправлять; находки для базы Ахима — в `docs/SUBMISSION.md`, отправка — решение владельца.
- В 11:00 WITA: скачать `~/strata`, `~/table`, логи → `logs/2026-09-02_ночь/`; снести ВМ; итог одним файлом.

ψ→
