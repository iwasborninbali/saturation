# Draft e-mail to Kovács, Nagy, Szabó (owner sends; English)

Subject: optimality of the HJSW deletion in the 2p×2p window — a proof and the equality cases

Dear Benedek, Zoltán and Dávid,

your paper "Randomised algebraic constructions for the no-(k+1)-in-line problem" states (Section 4 in v1: "essentially
details an optimal choice"; v2: "the largest no-3-in-line set S_2(c,p) inside S(W)") that the Hall–Jackson–Sudbery–Wild
deletion is optimal for a single modular hyperbola in the window G(p).  We have written a short note with a self-contained
proof of the statement for every c ∈ F_p^*: every subset of H(c,p) ∩ G(p) with no three collinear points has at most 3(p−1)
points (double counting over the lines of slope ±1 with three or four points; identity 2|L| + Z = 3(p−1)), together with a
classification of all extremal sets — there are exactly 9^s of them, s = [c is a QR] + [−c is a QR]; for p ≡ 3 (mod 4)
always 9, for p ≡ 1 (mod 4) 81 or a unique one (the HJSW set) according as c is a residue or not.  Everything is verified by
independent programs (all c for p ≤ 31; further p ≤ 101).

Before we post the note we would like to ask: do you have a written proof of the optimality assertion (in particular for the
cases with fixed classes, where your averaging bound |R| ≥ X/2 = p−1−s falls short by s), or was it stated from the
construction and the local optimality argument?  We want to attribute correctly: the assertion is yours; we believe the
proof and the equality classification are new, but we would rather ask than claim.  The draft is attached; comments are
very welcome.

With best regards,
Alex Komang (Bali)
(the note was produced by autonomous Claude agents under my direction; the AI role is disclosed in the note)
