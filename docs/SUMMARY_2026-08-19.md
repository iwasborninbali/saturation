# Итоги проекта на 19.08.2026 (простыми словами) — для хозяина

**Задача.** Сколько точек можно поставить на клетчатой доске N×N так, чтобы никакие три не лежали на одной прямой? Лучшая известная асимптотическая
конструкция (HJSW 1975): точки «модульной гиперболы» xy ≡ c (mod p) в окне 2p×2p — (3/2)·N точек; лучше никто не умеет 50 лет; задача 72 в списке Грина.

**Что доказано в заметке `paper/hjsw_window.pdf` (тег hjsw-note-v1.5):**
1. Одна гипербола в окне HJSW даёт РОВНО 3(p−1) точек — точная структура максимумов (число максимальных множеств 9^s), формула для любого окна 2p×2p,
   аналог для «никаких четырёх на прямой».
2. Среди всех коник только сдвинутые гиперболы дотягивают до 3(p−1) в окне 2p×2p.
3. Две гиперболы xy ≡ ±1: не больше (11/3+o(1))(p−1) — первая оценка ниже тривиальной 4(p−1) для объединения двух гипербол (v1.0–1.2).
4. Любая вторая гипербола xy ≡ k: точная теорема через «потенциал/буфер» на циклах строк‑столбцов и следствие α ≤ 4(p−1) − c√p/log⁵p при почти всех k (v1.4).
5. Ни один кубический график y = f(x) не достигает конструкции HJSW: α ≤ (11/8+o(1))·N < (3/2)N в любом окне 2p×2p (v1.5, доказано первым солвером,
   проверено вторым) — в русле замечания Грина «сформулировать точную гипотезу такого рода и посмотреть, доказывает ли она оптимальность HJSW».
6. Перестановочные мономы x^k (любое нечётное k ≥ 3, gcd(k,p−1)=1): α ≤ (1.474+o(1))·N < (3/2)N в любом окне; точные константы ≈ 1.375, 1.398, 1.402·N для
   k = 3, 5, 7, предел 1.4024·N (v1.6; первый солвер, проверено вторым).

**Основная статья `paper/no3inline_defects.pdf`:** 10 новых конфигураций с точной симметрией поворота (n = 33, 36, 37, 39 — четвёртая для 39 найдена
сегодня утром); 9 опубликованы Фламменкампом с указанием авторства, четвёртая отправлена ему сегодня.

**Что численно верно, но не доказано:** для любой пары гипербол максимум ≈ 3(p−1)+O(1) (данные до p = 29); линейная константа для всех k
(сертификат t = 1/m работает везде, доказательство упирается в «локальный закон» с нелепыми константами); сильная форма для кубик (LP ≈ 1.1·N).

**Оценка по нашей шкале (только вклад в науку): ≈ 2.8.** Путь к 3: общая теорема «перестановочные многочлены степени ≥ 3 не бьют HJSW» (в работе,
G3.7) и/или линейная константа для всех k; к 4–5 — доказать оптимальность 3/2 или побить её (не наш горизонт).

**Люди/машины:** три агента Claude (второй — Мак, первый — Хетзнер, третий — ВМ, молчит с 04:21), воркфлоу Sonnet для параллельных вычислений;
ВМ‑2/ВМ‑4 досчитывают развёртку n=41 (сегодня вечером), после чего — статья v1.0 и удаление машин.

---

# Дополнение: 09:00–12:30 WITA (после первой сводки)

## 1. Сильная форма для кубических графиков (заметка v1.9)
Гипотеза G3 из замечания Грина к задаче 72 закрыта: **каждый кубический график Y ≡ f(X) в коробке 2p×2p имеет ≤ (4/3+o(1))·N точек**
(для перестановочных кубик ≤ 1.326·N). Метод: покрытие прямыми наклона ±1 + паросочетание «хороших» 3‑точечных прямых семейства (1,3);
константы γ₁, γ₂ — численная квадратура явных кусочно‑полиномиальных интегралов (сказано прямо; запас над порогом > 10× ошибки).

## 2. Пара гипербол: 3.667 → 3.4616 и СТРУКТУРА (v1.10–v1.12) — главное за день
- Семь‑точечные блоки (v1.10): α ≤ 4(p−1) − 4m₈ − 4c₇, асимптотика 115/32 = 3.594.
- **Блочное разложение** (v1.11): точки распадаются на блоки по **сериям подряд идущих квадратичных вычетов**; ребро серии несёт ровно
  8 классов (32 точки); блоки замкнуты по строкам/столбцам; ни строка, ни столбец, ни ±1‑прямая не пересекает границу ⇒ оценка складывается
  из независимых блоков, LP — прямая сумма.
- **Закон спусков**: тип серии = множество спусков k независимых равномерных величин, плотность D_k(S)/k! (точное тождество через синусы).
- **Константа**: **α(P₋₁) ≤ 17865173/5160960 (p−1) = 3.4616(p−1)**; таблица оптимумов блоков собрана ТРЕМЯ независимыми способами — сошлись.
- **Граница метода**: работает только при k = ±1 (субкритический граф соседей); при других k — критическая перколяция, блоки ~p^{2/3}.
- **Устойчивость** (v1.12): одна гипербола распадается на орбиты Клейна; генерическая орбита жёсткая, исключительная даёт 9 вариантов ⇒
  **допустимое множество размера 3(p−1) − t отличается от максимума не более чем в t точках** (резко). Отсюда же счёт 9^s.

## 3. Пределы методов (честно)
LP по ВСЕМ прямым: 3.348 (p = 71), 3.406 (p = 101). SDP уровня 2: закрывает 8–17 % разрыва LP↔истина, доля падает с p.
Путь к 3(p−1)+O(1) — только структурный: H4′ «точка второй гиперболы стоит единицы дефицита ОТНОСИТЕЛЬНО оптимума первой» (взял коллега;
наклон 1 численно подтверждён при p ≤ 19, плато выигрыша симметрично), опираясь на устойчивость из п. 2.

## 4. Заказанное по «явлению целочисленности»
`docs/research/integrality/`: `phenomenon.py`, `holes.py` (H1–H8), `deep_research_brief_8_integrality.md`.
Закрыты численно: H1 (SDP слабо), H3 (устойчивость — ДА, доказана), H4 (наклон 1 — подтверждён).

## 5. Инфраструктура
41A sym9 на ВМ‑2: ~500 из 2375 (финиш к утру 20.08); затем таблица «41 полностью», тег основной статьи v1.0, удаление ВМ‑2.

## 6. Оценка по 5‑балльной шкале: ≈3.0 (утром 2.5)
Прибавили: G3, блочная структура + 3.4616, устойчивость, точные границы методов. Не прибавили: 3(p−1)+O(1) не взята;
теорема модели условна (короткие суммы характеров ниже Бёрджесса — Chang 2009, Pierce–Xu 2020 не покрывают нужный диапазон).
